# Clutter expansion evidence

- `pretraining_randomized_cylinders.png`: paired-\(\gamma\) SafeMPPI demonstrations on randomized cylinder maps.
- `pretrain_qualification.png`: trajectory-disjoint CFM loss and raw M=50/\(\gamma\) cylinder audit.
- `randomized_raw_curves.pdf`: raw temperature-one metrics on a disjoint randomized three-sphere bank.
- `fixed_scene_raw_gallery.png`: ten independent raw rollouts per \((\gamma,\mathrm{checkpoint})\) on one preregistered sphere map.
- `mechanism_multiview.png`: the three authoritative committed successes per displayed \((\mathrm{round},\gamma=0.5)\), each in its own randomized scene.
- `mechanism.mp4`: actual \(K=16\), selected \(B=4\), verifier-positive plans, executed first actions, and Adam-admitted successful windows.

`fixed_scene_raw_gallery.png` is the conditional-diversity evidence. Do not
interpret separation among mechanism rows as modes of one conditional policy:
those rows intentionally use different randomized scenes.

Verify the binary artifacts with `SHA256SUMS`.
